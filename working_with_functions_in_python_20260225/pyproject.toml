[project]
name = "working-with-functions-in-python"
version = "0.1.0"
description = "PyLadies Budapest - Függvények használata Pythonban"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
]
